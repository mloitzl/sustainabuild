// Ambient typings so the strict-mode TypeScript gateway can import the plain
// .mjs canonical SDL module (see viewer-sdl.mjs).
export const viewerSdl: string;
export const viewerSubschemaSdl: string;
